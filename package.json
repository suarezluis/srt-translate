{
  "name": "srt-translate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist",
    "serve": "live-server --port=3333 --entry-file=./dist/index.html --no-browser",
    "build": "tsc --outDir build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.2",
    "chalk": "^4.1.2",
    "live-server": "^1.2.2",
    "puppeteer": "^19.4.1"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "typescript": "^4.9.4",
    "dotenv": "^16.0.3"
  }
}
